import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'TICKETSHUB - Modern Event Ticketing Platform',
  description: 'Your premier destination for entertainment experiences. From blockbuster movies to live concerts, sports events to theater shows.',
  icons: {
    icon: '/ticketshub_logo.png',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}